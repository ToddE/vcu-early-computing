#
           if ( $file == x ) then
           echo -n "Exit grab option? (N) " 
           set resp=$< 

        	if (( $resp == y ) || ( $resp == Y )) then
               	goto begin
               	endif  

           endif

