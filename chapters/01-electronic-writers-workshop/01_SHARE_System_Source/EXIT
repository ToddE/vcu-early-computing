#EXIT
           if ( $flder == x ) then
           echo -n "Exit? (N) " 
           set resp=$< 

        	if (( $resp == y ) || ( $resp == Y )) then
               	goto begin
               	endif  

           endif

####endexit
